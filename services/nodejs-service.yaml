apiVersion: v1
kind: Service
metadata:
  name: "fill me"
  labels:
    name: "fill me"
  
spec:
  type: "fill me" # one of ClusterIP, LoadBalancer, NodePort, ExternalName
  ports:
    - name: "fill me" # name of the port
      targetPort: "fill me" # name of the port on the target pod
      port: "fill me" # The port that this service should serve on.

  selector:
    name: "fill me" # name of the targetted pod